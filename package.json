{
  "name": "ghost-workspace",
  "private": true,
  "version": "0.4.2",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.12.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "bash scripts/check-core-boundary.sh && biome check . --diagnostic-level=warn --error-on-warnings && pnpm -r --if-present lint",
    "verify": "bash .githooks/verify",
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10"
  }
}
