print_obsidian_readiness_steps() {
  cat <<'EOF'
  1. Open Obsidian and enable Settings > General > Command line interface.
  2. Install the upstream CLI skill:

  npx -y skills@latest add https://github.com/kepano/obsidian-skills \
    --global --yes --skill obsidian-cli

  3. Keep Obsidian open and verify both installation boundaries:

  obsidian version
  test -f ~/.agents/skills/obsidian-cli/SKILL.md

The Obsidian application package alone is not sufficient when its Arch build
omits the official standalone CLI. Follow https://obsidian.md/help/cli for the
official Linux registration. This root package hook cannot install a skill into
an arbitrary desktop user's home.
EOF
}

post_install() {
  cat <<'EOF'
Ghost is installed. The package does not edit Hyprland or Omarchy
configuration. Optional summon key and bar snippets are under
/usr/share/doc/ghost/shell-contrib/.

To relay the browser you already use, load this directory unpacked in Chromium:

  /usr/share/ghost/chromium-extension

Obsidian is shared persistent state for the owner and every ghost. Before
enabling Ghost, complete this required setup as the desktop user:
EOF
  print_obsidian_readiness_steps
  cat <<'EOF'

Then enable Ghost for the current graphical login with:

  systemctl --user enable --now ghostd.service ghost-shell.service

Other optional pi integrations are installed from upstream as this desktop
user; Ghost packages none of their CLIs or skills:

  npx -y firecrawl-cli@latest init --all --skip-auth
  hey skill install
  omarchy pkg add basecamp-cli
  basecamp skill install
  npm install -g @googleworkspace/cli
  npx -y skills@latest add https://github.com/googleworkspace/cli \
    --global --yes --skill gws-calendar gws-chat gws-docs gws-drive gws-forms \
    gws-gmail gws-keep gws-meet gws-people gws-shared gws-sheets gws-slides \
    gws-tasks gws-workflow

Exact skill selections, authentication commands, and upstream sources:

  /usr/share/doc/ghost/ARCH.md

Current Omarchy supplies hey through mise; run omarchy update if hey is missing.

Provider and MCP credentials are read from the Secret Service, so a
user-session provider such as gnome-keyring must be running and unlocked before
your first session:

  /usr/share/doc/ghost/docs/keyring.md

Ghost homes, credentials, browser profiles, and daemon state live in your home
directory. Pacman does not own or remove them.
EOF
}

post_upgrade() {
  cat <<'EOF'
Ghost was upgraded. Before restarting it, verify the required owner-level
Obsidian setup:
EOF
  print_obsidian_readiness_steps
  cat <<'EOF'

Then re-enable and restart the user services so an older default.target link is
replaced by the graphical-session.target lifecycle:

  systemctl --user reenable --now ghostd.service ghost-shell.service

No files under ~/ghosts, ~/.config/ghost, or ~/.local/state/ghost were changed
by the package upgrade.
EOF
}

pre_remove() {
  cat <<'EOF'
Before pacman removes the units, stop and disable them from the desktop user's
session in another terminal:

  systemctl --user disable --now ghost-shell.service ghostd.service

Removal still preserves every user-owned ghost home, credential, browser
profile, and daemon state file.
EOF
}

post_remove() {
  cat <<'EOF'
Ghost's system files were removed. User-owned ghost homes, credentials, browser
profiles, and daemon state were deliberately left in place. If either process
is still running in an existing login, stop it with:

  systemctl --user disable --now ghost-shell.service ghostd.service
EOF
}
